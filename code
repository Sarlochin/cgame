#include <fstream>
#include <iostream>

using namespace std;

int wertE [10][2];

int main()
{
  char namE[] = "Testname Bernd" ;
  int i = 5;

  int zahl;
  char nameo[20];

  //Creates an instance of ofstream, and opens example.txt

  ofstream fileout ( "example.txt" );	// Outputs to example.txt through file1

  fileout << namE << "\n"; //prints the name
  fileout << i << "\n";
  fileout << "\n\nEND OF FILE - nothing to see here-";
	
	  
	  
	// fout << flush; //write to file but keep it open

  
  fileout.close();	// write to file and close the file stream



  
  ifstream filein ( "example.txt" );	//Opens for reading the file

  //reads until there is a white space in the file
  
  filein.getline(nameo,20);	//Reads one string from the file
  filein >> zahl;

  

  cout <<"Name:\t" << nameo << "\n";
  cout <<"Zahl:\t" << zahl << "\n";

  cin.get();    // wait for a keypress

  // b_file is closed implicitly here



//  ifstream a_file ( "example.txt" );
//
//if ( !a_file.is_open() ) {
//  // The file could not be opened
//}
//else {
//  // Safely use the file stream
//}



}
